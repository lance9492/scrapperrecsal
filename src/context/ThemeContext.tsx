import React from 'react';

// This file is now just a placeholder since we removed dark theme
// We'll keep it for now in case we need to add other context later
export function ThemeProvider({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}